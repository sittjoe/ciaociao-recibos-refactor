{
  "name": "ciaociao-recibos-refactor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "e2e": "playwright test",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint \"src/**/*.js\" --max-warnings=0 || true",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  }
}
